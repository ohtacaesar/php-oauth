set $role "";

error_page 401 @login;

location @login {
    return 302 http://auth.localhost.dev/github?rd=${scheme}://${host}${request_uri};
}

location .auth {
    internal;
    proxy_pass http://auth.localhost.dev/auth;
    proxy_set_header ROLE $role;
    set $role "";
}
